# Apple Developer Account (for code signing and notarization)
# Get these from https://developer.apple.com
APPLE_ID=your-apple-id@email.com
APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
APPLE_TEAM_ID=XXXXXXXXXX

# Code Signing Certificate (optional)
# Export your certificate from Keychain and convert to base64
# CSC_LINK is the path to .p12 file or base64 encoded certificate
# CSC_KEY_PASSWORD is the password for the certificate
CSC_LINK=/path/to/certificate.p12
CSC_KEY_PASSWORD=certificate-password

# Distribution Options
UPLOAD_TO_S3=false
AWS_ACCESS_KEY_ID=your-aws-key
AWS_SECRET_ACCESS_KEY=your-aws-secret
S3_BUCKET=your-bucket-name

# Notarization (set to true when ready)
# This requires valid Apple Developer credentials
NOTARIZE=false
